---
name: dx
description: Operate Pierce's dev-experience repo (~/pierce-dx). Use when asked to sync, update, or apply his dx/dev setup, add something to his dev setup, set up a machine, or check for config drift.
---

Every dev-experience sync request routes through `~/pierce-dx`.

1. `cd ~/pierce-dx` and read `AGENTS.md`.
2. Execute the matching workflow: **capture** (machine → repo), **apply** (repo → machine), **extend** (add a new item), or **discover** (audit this machine for unsynced DX).
